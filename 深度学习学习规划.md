# Garbage-sorting-system 深度学习学习规划

## 1. `main.py` 中如何加载和使用 YOLO 模型

在 `main.py` 中，项目使用 `ultralytics` 的 `YOLO` 类加载已经导出的模型，代码如下：

```python
from ultralytics import YOLO
...
self.cls_ncnn_model = YOLO("model\yolo11n_cls_diy_v2_ncnn_model", task='classify')
```

这里的 `YOLO(...)` 不直接加载 `.pt` 权重文件，而是加载一个已经导出的 NCNN 模型目录：
- `model\yolo11n_cls_diy_v2_ncnn_model`
- 该目录里包含 NCNN 模型定义和权重文件，用于在 CPU / 轻量设备上推理。

`task='classify'` 表示这是一个分类模型，不是目标检测模型。代码里有一个检测模型的加载示例，但被注释掉了：

```python
# self.det_ncnn_model = YOLO("model/yolo11n_det_320_ncnn_model", task='detect')
```

### 模型是如何运行的

在 `update_frame()` 中，项目从摄像头读取每一帧，然后调用模型预测：

```python
results = self.cls_ncnn_model.predict(
    source=frame,
    imgsz=224,
    device="cpu",
    conf=0.25
)
```

这里的含义是：
- `source=frame`：直接把当前摄像头帧作为输入
- `imgsz=224`：输入图像尺寸为 224×224
- `device="cpu"`：使用 CPU 进行推理
- `conf=0.25`：分类置信度阈值，低于它的预测结果将被忽略

预测结果存储在 `results` 中，常见用法是：
- `results[0].probs.top1`：分类模型最可能的类别索引
- `results[0].probs`：包含每个类别的概率分布

在本项目中，后续逻辑会根据 `top1` 的类别索引判断垃圾类型，并驱动云台和传送带：
- 如果检测到垃圾，停止传送带
- 根据识别出的类别调用 `gimbal_work(...)` 倾倒垃圾
- 等待设备复位后继续下一帧检测

## 2. 这个项目里的模型训练流程是什么？我怎样才能在本地复现它？

这个仓库没有看到完整的训练脚本，只有 `export.py` 用于将已有 `.pt` 模型导出为 NCNN 格式。`export.py` 的内容是：

```python
from ultralytics import YOLO

model = YOLO("model\yolo11n_cls_diy_v2.pt")
model.export(format="ncnn", imgsz=224, task="classify")
```

这说明当前仓库的训练流程大概率是：
1. 在另一台电脑上训练得到 `model\yolo11n_cls_diy_v2.pt`。
2. 使用 `export.py` 将 `.pt` 模型转换成适合部署的 NCNN 格式。
3. 在 `main.py` 中加载 NCNN 模型目录进行推理。

### 复现训练的基本思路

如果你想本地复现训练，可以参考以下步骤：

1. 准备训练数据
   - 由于当前推理模型是分类模型，训练数据通常是“图像文件 + 类别标签”
   - 如果是 Ultralytics 分类任务，可使用如下数据结构：
     - 每个类别一个文件夹，文件夹名为标签名；或使用 `data.yaml` 配置文件
   - 本项目的类别名可能是：`food_waste`, `hazardous_waste`, `other_waste`, `recyclable_waste`

2. 安装训练环境
   - 在 PC 或服务器上安装 Python 3.11、PyTorch、Ultralytics
   - 你可以参考 `requirements.txt` 中的版本，尤其是 `ultralytics==8.3.19`

3. 使用 Ultralytics 训练
   - 示例命令（分类任务）：

```bash
yolo task=classify model=yolov8n_clf.pt data=data.yaml epochs=50 imgsz=224 batch=16
```

   - 如果你要使用仓库里已有的 `yolo11n_cls_diy_v2.pt`，则说明训练已经完成，复现训练要找到原始数据和训练配置。

4. 导出模型到 NCNN

```bash
python export.py
```

5. 在 Raspberry Pi 上测试推理
   - 将导出后的 NCNN 模型目录放在 `model\yolo11n_cls_diy_v2_ncnn_model` 下
   - 运行 `main.py`

### 这个项目是否支持完整训练？

从仓库内容看，当前项目更像“推理 + 硬件控制”工程，而不是“训练工程”。如果你想把这个仓库当成训练项目学习：
- 需要补充训练数据准备和 `data.yaml`
- 需要补充训练脚本或直接使用 Ultralytics CLI
- `export.py` 只是模型格式转换，不是训练脚本

## 3. Raspberry Pi 上测试分类模型的小实验步骤

### 3.1 目标说明

这个项目在 Raspberry Pi 上主要做的是“推理测试”，也就是用已训练好的模型识别摄像头画面中的垃圾类别。

### 3.2 环境准备

1. 在 Raspberry Pi 上安装系统，并配置 Python 环境
   - 建议使用 Raspberry Pi OS 64 位
   - 推荐使用 Miniconda 或 venv 创建独立环境

2. 安装依赖

```bash
pip install -r requirements.txt
```

3. 确认模型文件已经导出
   - `model\yolo11n_cls_diy_v2_ncnn_model` 目录存在
   - 若没有，可在另一台电脑上运行 `python export.py` 生成并复制到 Pi

4. 确认摄像头可用
   - 运行简单 CV 测试，检查 `cv2.VideoCapture(0)` 是否能打开

### 3.3 小实验步骤

1. 运行 `main.py`

```bash
python main.py
```

2. 观察程序启动后的日志
   - 看到“载入模型...”表示模型加载成功
   - 看到“摄像头启动成功”表示摄像头可用

3. 在程序界面中让摄像头对准不同垃圾样本
   - 观察分类结果是否显示正确类别
   - 观察程序是否控制云台、传送带等硬件动作

4. 如果你想只测试模型推理，可以用 `test.py` 或单独写一个最小推理脚本

### 3.4 什么是“Raspberry Pi 训练”？

“Raspberry Pi 训练”指的是在树莓派上用其硬件（CPU/可能的加速器）直接训练深度学习模型。通常应注意：

- 树莓派算力较弱，不适合训练大型模型
- 适合做小模型、小数据集的实验、概念验证
- 现实中更常见的做法是“在 PC/云端训练，Pi 上部署推理”

因此，本项目里最合理的方式是：
- 在PC端或服务器上训练模型
- 导出为 NCNN 格式
- 在 Raspberry Pi 上运行 `main.py` 进行推理

## 4. 学习建议与规划

1. 先理解 `main.py` 的推理流程：模型加载 → 摄像头读取 → 模型预测 → 根据预测结果驱动硬件。
2. 再理解 `export.py` 的作用：把 `.pt` 模型转换为 Pi 友好的 NCNN 格式。
3. 如果你想学习训练，先搞清分类任务的数据组织方式和 Ultralytics 训练命令；然后在 PC 上复现训练。
4. 之后可以尝试改造项目：
   - 增加更多垃圾类别
   - 尝试把分类模型换成目标检测模型
   - 记录预测结果并可视化分类概率

## 5. 你可以写入的下一个实践任务

- 任务 1：查看 `model/` 目录，确认每个模型文件夹对应的任务类型（分类 vs 检测）。
- 任务 2：在 PC 上用自己的垃圾图片集训练一个简单的分类模型。
- 任务 3：把训练好的模型导出为 NCNN 格式，复制到树莓派并运行 `main.py` 测试。
- 任务 4：把 `main.py` 中 `device="cpu"` 改成更适合树莓派的推理模式，并观察速度变化。
